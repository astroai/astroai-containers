# Source constraint for astroai-workload (cluster extra), consumed by
# `make lock-workload`. Installed into the base cadc venv (py3.13-safe:
# httpx/typer/canfar only — no Ray) so every session can run
# `astroai-workload cluster ensure|status|stop|scale|dashboard`.
#
# Pin is a fixed commit SHA, NOT a git tag and NOT a package version.
#
# Bump deliberately: pick a SHA on `main` that you have tested, replace the
# line below, then `make lock-workload`. Never edit the lockfile by hand.
astroai-workload[cluster] @ git+https://github.com/astroai/astroai-workload.git@b3c061b1149cdca478005ebf9ee5d294bd761821
